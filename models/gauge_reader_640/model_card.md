# Analog Gauge Reader

This package contains ONNX exports of the yibiao_v3 gauge detector and YOLOv8 pose model.
The detector identifies gauge ROIs. The pose model provides pointer, scale-start and scale-end
geometry used to calculate a reading. The model binaries are intentionally excluded from Git.
